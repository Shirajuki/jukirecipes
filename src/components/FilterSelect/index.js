import React from "react";
const FilterSelect = ({ value, setValue }) => {
  return (
    <p>
      Filter:
      <button></button>
      <button></button>
      <button></button>
    </p>
  );
};
export default FilterSelect;
